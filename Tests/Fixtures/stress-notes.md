# Stress notes

A Markdown sibling of the HTML stress test. It exists so the link graph has a
real Markdown node to draw an edge to, and so activating a local link inside the
web view opens a marc tab instead of navigating the page.

See [the stress test](html-stress-test.html) and
[the torture file](html-stress-torture.html).
